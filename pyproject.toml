[tool.poetry]
name = "fyers-api-helper"
version = "0.1.0"
description = "This is a helper package for official Fyers API"
authors = ["Aditya Kale <dev.adityakale@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
